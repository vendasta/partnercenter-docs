---
title: Order and launch a service
sidebar_position: 4
description: "Find Vendasta Services products in the Marketplace, complete the fulfillment form, prepare the access the team needs, and track the build to launch."
---

import KnowledgeCheck from "@site/src/components/KnowledgeCheck";
import CourseProgressBar from "@site/src/components/CourseProgressBar";
import SectionFeedback from "@site/src/components/SectionFeedback";
import InlineHighlighter from "@site/src/components/InlineHighlighter";
import MarkComplete from "@site/src/components/MarkComplete";
import LessonHeader from "@site/src/components/LessonHeader";
import LessonFooter from "@site/src/components/LessonFooter";
import LessonSection from "@site/src/components/LessonSection";
import LabCallout from "@site/src/components/LabCallout";
import LabChecklist from "@site/src/components/LabChecklist";

<InlineHighlighter courseId="order-and-launch-a-service" site="vendasta_learn">

<CourseProgressBar courseId="order-and-launch-a-service" site="vendasta_learn" />

<LessonHeader
  difficulty="Beginner"
  topics={["Client delivery", "Marketplace", "Orders"]}
  time="about 12 minutes"
  lab
  required={["Partner Center admin or salesperson access to share fulfillment forms"]}
  pathName="Work with Vendasta Services"
  step={4}
  totalSteps={5}
  outcomes={[
    "Find any Vendasta Services product in the Marketplace",
    "Complete and share a fulfillment form so work starts without a delay",
    "Prepare the connections, credentials, and access a service needs before its pre-launch call",
    "Show a client where their project status lives",
  ]}
/>

## Find the products

*You are here: Partner Center. Everything in this section happens in your workspace.*

Vendasta Services products do not carry "Vendasta Services" in the title, so searching by name misses them. Filter by vendor instead, and clear the vendor chip the page starts with: two vendor filters combine rather than replace each other, and the result is an empty catalog.

<LabCallout title="filter the catalog by vendor">
Go to `Marketplace` → `Discover Products`.

<LabChecklist
  storageKey="order-and-launch-lab-1"
  steps={[
    <>Set <strong>Explore</strong> to <code>All</code>, or clear the <code>Vendor is Vendasta</code> chip. The page applies it by default, and leaving it on returns nothing.</>,
    <>Select <code>Add filter</code>, choose <code>Vendor</code> → <code>Vendasta Services</code>, then <code>Apply</code>.</>,
    <>Scan the results, so you know what is actually available to order.</>,
  ]}
  confirm={<>The Vendasta Services catalog is in front of you in one list, including the products whose names never say "Vendasta Services".</>}
/>
</LabCallout>

<LessonSection>

## Complete the fulfillment form

Ordering works exactly like any other Marketplace product. What is different is what comes after: a **fulfillment form** that gives the team the detail it needs to deliver.

- **Order first or fill the form first.** Either sequence works. After purchase you are directed to the form automatically.
- **You can save progress and come back.** Forms hold drafts, so gathering a missing brand asset does not cost you the work already entered.
- **You can share the form with a Business App user.** They receive an email and can fill in the parts only the client knows. Partner Center admins and salespeople can share.
- **Submitting notifies the team.** An incomplete form generates in-app and email reminders instead, and the work waits.

The form's questions differ by service, and the fulfillment team's guide for each one lists them; what is the same every time is how you share and submit it.

<LabCallout title="share the form, then submit it">
Go to the order's fulfillment form. It opens automatically after purchase, and you can return to it later at `Commerce` → `Orders` → open the order → `Fulfillment forms`.

<LabChecklist
  storageKey="order-and-launch-lab-2"
  steps={[
    <>Save a draft, so nothing is lost while you gather what you are missing.</>,
    <>Share it with a Business App user for the parts only the client knows. They receive an email, and both admins and salespeople can share.</>,
    <>Note on the form itself if only certain people should hear from the fulfillment team.</>,
    <>Submit. Submitting is what notifies the team; an incomplete form generates reminders instead, and the work waits.</>,
  ]}
  confirm={<>The team has been notified and the clock has started, rather than the order sitting on a form nobody finished.</>}
/>
</LabCallout>

Website orders have a second requirement worth flagging: complete the Website Creation and Content Form **and** activate the product. Both steps are needed, and skipping either one delays the build.

</LessonSection>

## What happens in the first 48 hours

Every contact listed on the order form receives an order confirmation email within 48 hours. It carries an order reference number, the information fulfillment still needs, and, where the service includes one, the details of your pre-launch call.

If only certain people should hear from the team, note that on the order form itself.

<LessonSection>

## The pre-launch call

Most services open with a call before work begins. Reputation, Social, Blogs, Websites, and Digital Ads all include one. Listings does not; that work starts from the fulfillment form.

The call covers three things: a welcome and a rundown of what happens when, the connections and account authorizations the service needs inside Business App, and a content questionnaire for anything the fulfillment team still needs from you.

Managed Ads Campaign adds a **post-launch call** as well, once the campaign is built and ready to go live, to set expectations for performance and optimization.

</LessonSection>

## What to have ready

Anything you can arrange before the call shortens the runway to launch. Requirements differ by service.

| | Listings | Reputation | Social | Blogs | Websites | Digital Ads |
|---|---|---|---|---|---|---|
| **Pre-launch call** | No | Yes | Yes | Yes | Yes | Yes |
| **Business App connections** | Yes | Yes | Yes | No | No | Yes |
| **External credentials** | No | Yes | Yes | No | Yes, or registrar access | No |
| **Digital Agency access** | Yes | Yes | Yes | No | Yes, or credentials | Yes |
| **Business days to launch after assets arrive** | 3 | 1 to 2 | 5 | 5 | Varies by build | 1 to 10 |

Two notes on that table. Website services need either external credentials or delegate access at the registrar, not both. And complex listing claims, including Google Business Profile claims and optimization, can run to 90 days when a platform requires additional verification, so quote listings work with that possibility in view.

Websites and Digital Ads are the two lines where the product you picked matters more than the line itself. An **Express** website build delivers a first draft of up to five pages within one business day, implements revisions in two to five business days, and goes live within one business day of your final approval, targeting a full launch inside 15 days. **Templated** and **Accelerated Templated** builds set their schedule at the onboarding call. On the ads side, **Express Ads** goes live in minutes, while a **Managed Ads Campaign** launches over roughly 30 days. The [Vendasta Services documentation](/vendasta-services) carries the detail for each specific product.

<LessonSection>

## Track it to launch

Every service publishes a tracker showing the tasks in flight, expected completion dates, and notes from the team. You watch it from `Fulfillment` → `Projects`; your client watches the same work from their own Business App, which takes a recurring status question off your desk.

<LabCallout title="find the tracker, then decide who sees it">
Go to `Fulfillment` → `Projects` for your own view, then the client's `Business App` → `Projects` for theirs.

<LabChecklist
  storageKey="order-and-launch-lab-3"
  steps={[
    <>In <code>Fulfillment</code> → <code>Projects</code>, find the project and read its status, assignee, and due date.</>,
    <>Open the client's Business App and look for <code>Projects</code> in the navigation. It appears once they have a project, so a client with none will not see the tab at all.</>,
    <>Decide whether clients should see it. The switch is <code>Administration</code> → <code>Customize Business App</code> → <code>Pages</code> → <code>Projects</code>, and it applies to every account in the market rather than to one client.</>,
    <>If a single project should stay hidden, ask the team to hide that one.</>,
  ]}
  confirm={<>You know what your clients see and what you see, and the visibility decision was made deliberately rather than by default.</>}
/>
</LabCallout>

</LessonSection>

<SectionFeedback courseId="order-and-launch-a-service" site="vendasta_learn" section="content" />

<KnowledgeCheck courseId="order-and-launch-a-service" site="vendasta_learn"
  sessionSize={3}
  intro="Three quick questions on finding the products, what starts the work, and where project status lives."
  questions={[
    {
      type: 'mcq',
      question: 'You cannot find a service product by searching its name in the Marketplace. What is the reliable way to see the full catalog?',
      options: [
        'Search for "Vendasta Services" in the product title',
        'Add a filter for Vendor and choose Vendasta Services',
        'Browse the Attract, Convert, and Engage categories in order',
        'Ask Support on Demand for the product list'
      ],
      correctIndex: 1,
      explanation: 'These products do not carry Vendasta Services in the title. The Vendor filter on Discover Products returns all of them at once.'
    },
    {
      type: 'mcq',
      question: 'You placed an order but have not submitted the fulfillment form. What happens?',
      options: [
        'The team begins with default settings and adjusts later',
        'The order is cancelled after 48 hours',
        'The work waits, and you receive in-app and email reminders',
        'The form is completed automatically from the account profile'
      ],
      correctIndex: 2,
      explanation: 'Submitting the form is what notifies the team. An incomplete form generates reminders and the work waits until it arrives.'
    },
    {
      type: 'mcq',
      question: 'A client asks for a status update on their website build. Where do you point them?',
      options: [
        'Business App, under Projects',
        'Partner Center, under Task Manager',
        'The order confirmation email thread',
        'The Marketplace product page'
      ],
      correctIndex: 0,
      explanation: 'Every service publishes a tracker in Business App under Projects, with tasks, expected completion dates, and notes. You can hide the tab in Administration if you would rather field the question yourself.'
    },
    {
      type: 'mcq',
      question: 'Which service line does not include a pre-launch call?',
      options: [
        'Social',
        'Websites',
        'Listings',
        'Blogs'
      ],
      correctIndex: 2,
      explanation: 'Reputation, Social, Blogs, Websites, and Digital Ads all include a pre-launch call. Listings work begins from the fulfillment form.'
    }
  ]}
/>

<LessonFooter to="/learn/vendasta-services/run-the-work-over-time" pathName="Work with Vendasta Services" step={4} totalSteps={5} />

</InlineHighlighter>
<MarkComplete courseId="order-and-launch-a-service" site="vendasta_learn" />
