import React from 'react';

export interface CommentBoxProps {
  prompt?: string;
  courseId?: string;
  site?: string;
  variant?: 'discussion' | 'reflection';
}

export default function CommentBox(_props: CommentBoxProps) {
  return null;
}
