---
title: "How do I update email settings?"
sidebar_label: Update email settings
description: "Set sender name, reply address, and sender domain in Partner Center email settings, and add SPF, DKIM, and DMARC records so Campaigns emails avoid spam."
sidebar_position: 3
brand: vendasta
product: marketing
audience: partners
content_type: how-to
kb_id: KB-00076
answer_snippet: "To update email settings in Partner Center, go to Marketing → Email settings, set your sender name, reply address, and sender domain, then add the SPF, DKIM, and DMARC records to your DNS so your Campaigns emails are less likely to be flagged as spam."
doc_owner: marketing
review_frequency: 3-months
last_reviewed: 2026-09-25
last_reviewed_by: haleyserrano
---

To update email settings in Partner Center, go to `Marketing` → `Email settings`, set your sender name, reply address, and sender domain, then add the SPF, DKIM, and DMARC records to your DNS so your Campaigns emails are less likely to be flagged as spam.

Update the following settings to help your email campaigns pass spam filters and reach your prospects.

:::note
To prevent emails from being sent on someone's behalf unintentionally, email service providers check the digital records of incoming mail. These records—including Sender Policy Framework (SPF), Domain Keys Identified Mail (DKIM), and Domain-based Message Authentication, Reporting & Conformance (DMARC)—act as digital handshakes agreeing to send an email on someone's behalf. You can prevent your emails from being flagged as spam by updating your Domain Name System (DNS) record with the correct SPF, DKIM, and DMARC entries. Updating these entries creates an agreement between Vendasta and your domain and allows us to send email campaigns on your behalf.
:::

<div style={{position: "relative", paddingBottom: "56.25%", height: 0}}>
  <iframe 
    style={{position: "absolute", top: 0, left: 0, width: "100%", height: "100%"}} 
    src="https://www.loom.com/embed/fc5735ac6e79472f9c3f0263d81b8a30" 
    frameBorder="0" 
    allowFullScreen
  />
</div>

To update email campaign settings:

1. Go to `Partner Center` → `Marketing` → [`Email settings`](https://partners.vendasta.com/campaign-settings). The page has three stacked cards: `Sender and reply settings`, `Advanced email settings`, and `Email domains`.
2. In the `Sender and reply settings` card, set `Sender name` and `Reply Address`, then select `Save` on that card.
   :::note
   **Note:** When a salesperson is assigned to an account, the salesperson's email address and name is used instead of these settings.
   :::
3. In the `Advanced email settings` card, set `Sender domain` (choose `Use my own email domain` to send from your own domain) and `Email address`, then select `Save` on that card.
   :::warning
   Each card has its own `Save` button. Saving one card does not save the other, so press both.
   :::
4. In the `Email domains` card, check the four records under your domain: one SPF (TXT), two DKIM (CNAME), and one DMARC (TXT). Each row has a `Current value` column showing the value found at your DNS host, or `Not found` in red if it has not been detected. The domain's own row header carries the overall status, and a `Domain authorization required` banner stays on the page until every record lands.
5. For each record showing `Not found`, update your DNS record with the correct entry. For assistance, you can contact your DNS service provider.

:::info Scope
These settings drive Campaigns and other integrated services. Emails sent through Conversations are not affected.
:::

![Email settings screenshot](./img/email-settings/email-settings-screenshot.png)

<a 
  style={{
    fontSize: "16px", 
    fontWeight: "bold", 
    color: "#ffffff", 
    backgroundColor: "#33ace2", 
    textDecoration: "none", 
    borderRadius: "5px", 
    padding: "10px 30px 9px 30px", 
    border: "1px solid #33ACE2", 
    display: "inline-block", 
    textAlign: "center"
  }} 
  href="https://partners.vendasta.com/campaign-settings" 
  target="_blank" 
  rel="noopener"
>
  Update settings
</a>

## How do I keep my emails from being flagged as spam? {#important-notes}

Setting Vendasta as a trusted service provider does not guarantee that emails will not be flagged as spam. To ensure your emails are delivered successfully, check the content and subject lines of your emails. Make sure to avoid:

- Colored text
- Text in all caps
- Attachments
- Exclamation marks
- Promotional words and phrases (such as "make money," "save money," "cheap," and "act now")
- Profanity and explicit content

These may cause your emails to be flagged as spam.

If you do not own a custom domain, consider purchasing one through a domain registrar such as GoDaddy. You can use your custom domain to create and maintain a professional email address.

## Frequently asked questions

<details>
<summary>How do I change the sender name and reply address on my emails?</summary>

Go to `Partner Center` → `Marketing` → `Email settings`. In the `Sender and reply settings` card, set `Sender name` and `Reply Address`, then select `Save` on that card. When a salesperson is assigned to an account, the salesperson's name and email address are used instead.

</details>

<details>
<summary>Which DNS records do I need to add to authorize my domain?</summary>

Add one SPF (TXT) record, two DKIM (CNAME) records, and one DMARC (TXT) record at your DNS host. The `Email domains` card shows `Not found` in red for any record that hasn't been detected yet. For help adding records, contact your DNS service provider.

</details>

<details>
<summary>Do these settings affect emails sent through Conversations?</summary>

No. These settings drive Campaigns and other integrated services. Emails sent through Conversations are not affected.

</details>

<details>
<summary>Does authorizing my domain guarantee my emails won't go to spam?</summary>

No. Setting Vendasta as a trusted service provider does not guarantee that emails will not be flagged as spam. Check the content and subject lines of your emails and avoid colored text, all caps, attachments, exclamation marks, promotional phrases, and profanity.

</details>

<details>
<summary>Why didn't my changes save?</summary>

Each card on the `Email settings` page has its own `Save` button. Saving one card does not save the other, so select `Save` on both the `Sender and reply settings` and `Advanced email settings` cards.

</details>
